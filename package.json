{
  "name": "car-repair-traceability",
  "version": "0.1.0",
  "private": true,
  "description": "Blockchain-backed verification and warranty traceability for vehicle repair records",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:geth": "npm run compile && ts-node scripts/deploy.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^22.10.0",
    "hardhat": "^2.22.19",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
